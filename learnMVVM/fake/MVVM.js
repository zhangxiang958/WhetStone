class MVVM {
    constructor (opts) {
        this.opts = opts;
        this._data = opts.data || {};
        this.el = opts.el;


    }

    _proxyData (key, setter, getter) {
        
    }

    $watch (key, cb) {
        
    }

    $set (key, value) {
        
    }

    $delete (key) {}
}